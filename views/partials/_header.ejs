  <h2>TinyApp</h2>
  <% if (currentUser) { %>
    <table>
      <tr>
        <td>
          <p>Logged in as <%= currentUser.email %></p>
        </td>
        <td>
          <form method="POST" action="/logout">
            <button>LOGOUT</button>
          </form>
        </td>
      </tr>
    </table>
  <% } else { %>
    <p>Please <a href="/login">Login</a> or <a href="/register">Register</a> to create short URLs.</p>
  <% } %>