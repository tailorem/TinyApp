<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>TinyApp</title>
</head>
<body>
  <%- include('partials/_header') %>

  <ul>
    <% for(url in urls) { %>
    <li><%= `${url}: ${urls[url]}` %></li>
    <% } %>
  </ul>
  <p>Shorten your URL <a href="/urls/new">here</a>!</p>
</body>
</html>