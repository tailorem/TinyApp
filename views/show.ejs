<!DOCTYPE html>
<html lang="en">
  <%- include('partials/_header') %>
  <body>
    <% if (currentUser) { %>
    <table>
      <tr>
        <td>
          <p><a href="<%= longURL.longURL %>"> <%= `localhost:8080/${shortURL}` %></a> → </p>
        </td>
        <td>
            <form method="POST" action="/urls/<%= shortURL %>/edit" style='display:inline;'>
              <input type="text" name="newURL" value="<%= longURL.longURL %>" style="width: 250px;">
              <button type="submit">UPDATE</button>
            </form>
        </td>
      </tr>
    </table>
    <p><a href="/urls/new">Create a new short URL</a> <a href="/urls/">View my short URLs</a></p>
    <% } %>
  </body>
</html>

